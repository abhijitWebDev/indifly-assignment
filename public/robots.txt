# Allow all search engines to crawl the site
User-agent: *
Allow: /

# Disallow crawling of API routes and private directories
Disallow: /api/
Disallow: /_next/
Disallow: /admin/

# Sitemap location (will be automatically resolved to your domain)
Sitemap: /sitemap.xml

# Crawl-delay (optional - helps prevent server overload)
# Crawl-delay: 1
